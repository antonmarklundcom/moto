# R0 — Research (catalog, competitors, vocabulary)

## Built
- `docs/research/catalog.md`, `competitors.md`, `vocabulary-check.md` (H-9 checklist).
- Honda 0 → 12 active of 14 (DIESA S.A. = official distributor; DIESA ads on Clasipar, ABC Color / La Nación launch press). `cg-150-titan`, `cb-125` stay inactive with `[VERIFICAR]`.
- New brand Star (ALEX S.A., made in PY): 2 models from `star.com.py` product pages. Kenton 1 → 6 from `kenton.com.py` product pages.
- Catalog now 8 brands (7 active), 37 models (35 active). Model → category proposal in `catalog.md` §5 (`scripts/` is not R0's).
- `src/db/seed-data/seed-data.test.ts`: no reserved/duplicate slugs, model → existing (and active) brand, inactive ⇔ `[VERIFICAR]`, no pre-R0 slug removed.

## Decisions
- Activation criterion: "sold in Paraguay, new or used" (not the current lineup), minimum evidence = official distributor ad, brand's PY product page, or PY press. A private seller's ad alone is not enough.
- `WebFetch` was blocked for every domain in this session (egress policy, confirmed via `/root/.ccr/README.md`; even Wikipedia). Not an escalation item: degraded to `WebSearch` (titles, URLs, snippets) and said so in `catalog.md` §0.
- Did not guess names: CB160 / "Africa 1100" (DIESA ad title) and Star's reseller-only models are left out.
- Opus review of the Sonnet draft: removed two unsourced market claims (Star "highest volume", Kenton "moto nacional"), corroborated CG 110 / Wave / Tornado / CB500X by ad titles, added Navi 110 + CB1 125, flagged CRF 250F as weakest evidence, added Star's category words to the vocabulary checklist.

## Known issues
- Re-confirm against live sites before B8 imports real stock: `hondamotos.com.py` (esp. CRF 250F, CB 125 vs CB1 125), `star.com.py/catalogo`, Yamaha Ray ZR/FZ.
- Category names: Star uses Cobrador / Motoneta / Pistera / Todoterreno / Carga, not Naked / Cub. Renaming a category **slug** is a URL change (escalate); settle it via H-9 before B1 publishes `/motos/tipo/*` indexable.
- `categoryId` is still NULL for every model until a phase owning `scripts/seed-catalog.ts` applies §5.

## Link-pass wishes
- None (lane 0).

Verification: seeds ×2 on `moto_dev` and ×2 on `moto_test`, identical totals; verify green on the PR #13 head (pre-push hook).
