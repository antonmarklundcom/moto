# C2 — Hardening, E2E, closing report

## Built
- `tests/e2e/test-plan.spec.ts`: the TEST_PLAN §4 cases no phase covered end to end (WhatsApp 302, honeypot, lead with any CRM state, honest empty search).
- `tests/e2e/a11y.spec.ts`: axe on 10 pages. `axe-core` is now a pinned devDependency; it was already installed through eslint-plugin-jsx-a11y.
- `tests/e2e/security.spec.ts`: headers, and a POST sweep of every admin/cron route without a session.
- `scripts/dev-fixtures.ts`: the reset failed with FK `lead_deliveries → leads` once the mock CRM had delivered a lead. It now deletes the deliveries first.
- `docs/closing-report.md`. `KNOWN-ISSUES.md`: corrected which env vars need a restart and which need a rebuild.

## Decisions
- Lighthouse and axe run from the scratchpad and the E2E suite. Lighthouse is not committed: one-off measurement, per the protocol's polish cap.
- No watcher Routine existed, so there was nothing to delete.
- No fixes were needed: 0 axe violations, and every Lighthouse performance score was ≥ 98.

## Known issues
- One E2E run: the planilla-download test timed out waiting for the download. It passed alone and in the two following full runs; not reproduced.
- Back-to-back full E2E runs from one local IP hit the real lead limit (10 per 10 min). Restart `next start` between runs.
- Codex/Fable pre-launch audit due before `SITE_NOINDEX=false` (owner, `BUILD_PLAN.md` §8).

Verification: `npm run verify` green; E2E 55/55 on a production build with the mock CRM.
